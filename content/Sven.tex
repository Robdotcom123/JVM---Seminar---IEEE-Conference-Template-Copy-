https://dzone.com/articles/introduction-to-java-bytecode
Fragen: Ist linking, classpath relevant?  
Bei uns ist angekommen, dass wir gemeinsam úber das paper schauen und das zusammen besprechen
Wie wichtig sind ihnen Buchquellen anstatt quellen aus dem Internet? 

Können wir in den kommenden Tagen einen Termin machen um gemeinsam über unseren Vorschlag für eine Gliederung des Papers zu sprechen?


Structure:

What is Java-Bytecode?
- bytecode instruction set (one byte instruction "words", limited to 256 instructions, 200 currently in use)
- many languages can be compiled to java bytecode
- classes of programming languages running on the jvm
- interpreted by the JVM (runs the bytecode and performs optimizations at runtime) %https://blog.jdriven.com/2019/11/hotspot-jvm-optimizations/
  - runs on many platforms

how bytecode is interpreted
- Interpreted on a stack machine and register machine
- frames (created on method invocation)

datatypes in the bytecode

bytecode instruction set
- naming convention for instructions
- groups of commands (list from wikipedia)
  - Load and store (e.g. aload_0, istore)
  - Arithmetic and logic (e.g. ladd, fcmpl)
  - Type conversion (e.g. i2b, d2i)
  - Object creation and manipulation (new, putfield)
  - Operand stack management (e.g. swap, dup2)
  - Control transfer (e.g. ifeq, goto)
  - Method invocation and return (e.g. invokespecial, invokedynamic , areturn)

bytecode for a example program written in Java

%// All instructions
%https://en.wikipedia.org/wiki/List_of_Java_bytecode_instructions




Gehört die Struktur um den Bytecode (Stackgröße, Flags, Descriptor,
```
public static void main(java.lang.String[]);
  descriptor: ([Ljava/lang/String;)V
  flags: (0x0009) ACC_PUBLIC, ACC_STATIC
  Code:
    stack=2, locals=4, args_size=1
       0: iconst_1
       1: istore_1
``) zum Scope des Papers?



public static void main(java.lang.String[]);
  descriptor: ([Ljava/lang/String;)V
  flags: (0x0009) ACC_PUBLIC, ACC_STATIC
  Code:
    stack=2, locals=4, args_size=1
       0: iconst_1
       1: istore_1
       2: iconst_2
       3: istore_2
       4: iload_1
       5: iload_2
       6: invokestatic  #2         // Method calc:(II)I
       9: istore_3
      10: return

static int calc(int, int);
  descriptor: (II)I
  flags: (0x0008) ACC_STATIC
  Code:
    stack=6, locals=2, args_size=2
       0: iload_0
       1: i2d
       2: ldc2_w        #3         // double 2.0d
       5: invokestatic  #5         // Method java/lang/Math.pow:(DD)D
       8: iload_1
       9: i2d
      10: ldc2_w        #3         // double 2.0d
      13: invokestatic  #5         // Method java/lang/Math.pow:(DD)D
      16: dadd
      17: invokestatic  #6         // Method java/lang/Math.sqrt:(D)D
      20: d2i
      21: ireturn